# Shared dependencies for the released open-source workflows.
#
# We recommend installing a matching PyTorch build (CUDA/CPU) first, then run:
#   pip install -r requirements.txt
#
# This file consolidates dependencies across:
# - Worldmodel/action_decoder/src (latent-to-action training & batch inference)
# - Worldmodel/runtime (RL workflows and shared world-model deps)
# - infer (online API service)

-r Worldmodel/action_decoder/src/requirements.txt
-r Worldmodel/runtime/requirements.txt

fastapi
uvicorn
pydantic
